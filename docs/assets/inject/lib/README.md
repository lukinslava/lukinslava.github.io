`ui-sounds.js` — procedural UI sounds (Web Audio API, no audio files), copied unchanged from
https://github.com/mishanaer/sound (`assets/ui-sounds.js`). The repository has no license file; all credit to its author.
